#include<stdio.h>
#include<conio.h>
int main()
{
int n,i,sum=0;
clrscr();
printf("Enter an integer:");
scanf("%d",&n);
for(i=1;i<=n;i++)
{
if(n%i==0)
{
printf("\n divisor is : %d",i);
sum=sum + i;
}
//sum++;
}
printf("\n sum of all divisior is : %d",sum);
getch();
return 0;
}