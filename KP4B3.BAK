#include<stdio.h>
#include<conio.h>
int main()
{
int n,i,sum=0;
clrscr();
printf("Enter an integer:");
scanf("%d",&n);
for(i=1;i<n;i++)
{
if(n%i==0)
{
printf("\n %d is divisor",i);
sum=sum+i;
}
}
printf("\n sum of divisior is : %d",sum);
{
if(n%sum==0)
{
printf("\n %d is perfect number",n);
}
else
{
printf("\n %d is not perfect number",n);
}
}
getch();
return 0;
}