#include<stdio.h>
#include<math.h>
int main()
{
int dividant,divisor,quotient,remainder;
printf("enter a divident :");
scanf("d", &dividant);
printf("\n enter a divisor :");
scanf("%d",&divisor);
quotient= dividant/divisor;
remainder= dividant % divisor;
printf("quotient = %d",quotient);
printf("remainder = %d", remainder);
getch();
return 0;
}
